{
  "name": "casamaquinas",
  "version": "1.0.0",
  "description": "Sistema Casa das Máquinas - Frontend e Backend unificados",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mercadopago": "^1.5.14",
    "mongoose": "^7.0.3",
    "mysql2": "^3.14.0",
    "nodemailer": "^6.9.1",
    "sequelize": "^6.37.7",
    "validator": "^13.9.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "mongodb-memory-server": "^10.1.4",
    "nodemon": "^2.0.22"
  }
}
